# Final cost diagnostic, declared before execution

The n=8/n1=1 fully race/refined partner profiles also crossed the 5% fallback
threshold after two completed pairs in each extension-1 matchup. Do not treat
those few wrapped outcomes as evidence of the deeper model's playing strength.

Keep the improved real root (race/refine, n=40, n0=8), but give modeled L1 minds
one fixed bundle of n1=2. This uses the same selection implementation with its
Fixed rule: the distinction is explicit configuration, not an unshared copy.
It isolates partner modeling without recursively multiplying refinement work.
This is a budgeted approximation to L1, not a claim that the modeled partner
executes the complete forty-world phone procedure.

Fresh panel 740600–740624, 25 paired deals each, with and without voids, against
the corresponding full L1 anchor. Same independent player manifests, wall
allowance, parallel pool, and technical stop rule. This is the final planned
cost diagnostic in this session; any residual strength uncertainty is reported,
not chased with additional outcome-selected experiments.
