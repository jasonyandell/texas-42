[package]
name = "walt-wasm-spike"
version = "0.1.0"
edition = "2021"
license = "UNLICENSED"
publish = false

[dependencies]
walt-core = { path = "../walt-core" }
walt-m3-probe = { path = "../walt-m3-probe", default-features = false }
num-rational = { workspace = true }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
float_arithmetic = "deny"
