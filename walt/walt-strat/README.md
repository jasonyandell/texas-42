# walt-strat

Owns the operators registry of the crate map. S2 lands the first operator:
worldwise perfect-information symbolic parametric backward induction
(v0.4 §9.9) -- focal-team nodes take the pointwise envelope maximum, opposing
nodes the minimum, leaves are affine trick increments -- together with the
fiber census that partitions worlds by parametric root-Q vector, by baseline
value at zero, and by optimal-action correspondence (§14.2's census
vocabulary). A degenerate fixed-field evaluator covers kernels where the
viewer never chooses after the root (every horizon-2 kernel); it refuses,
rather than misprices, anything needing the general hidden fixed-field solve.

**Imports: `walt-core`, `walt-kernel`, `walt-geom`** -- the strict import
direction of v0.4 §16.2.

Still to come here (S3): information partitions over decision nodes (§10.1),
policies as maps from information-state ids, and the H/C/F treatment
operators with their information prices, cross-checked against the exp4
vectors (7/19, 177/131, 19/105, the identically-zero continuation price).

Validated against the v0.4 §14.2 trick-6 experiment (exploratory tier, used
as regression pins, never axioms): the fixed-field lines, the 8/4/3 census
with class sizes (26,22,16,12,8,2,2,2), and the eight-world boundary tie
resolving at zero-plus.
