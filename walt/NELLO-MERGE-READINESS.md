# Nel-O shared player: merge preparation

2026-09-25 · **Exploratory engineering evidence.**
Research synthesis: [Walt and Nel-O](../wiki/walt-nello.md).
Fresh receipts: [nello-merge-2026-09-25](probes/nello-merge-2026-09-25/), with
tested source and artifact identities in [manifest.json](probes/nello-merge-2026-09-25/manifest.json).

## Integration order

1. [PR #90](https://github.com/jasonyandell/texas-42/pull/90),
   `codex/nello-player` → `main`: doubles-suit mechanics and the explicit
   three-active-seat, zero-trick contract. Updated over `main` at `ee44cb56`;
   the only merge conflict was the rewritten wiki index. The new index and
   the Nel-O implementation link are both retained. A pre-existing Clippy
   complaint in the touched auction adapter is fixed with the equivalent
   `is_multiple_of(2)` parity predicate.
2. [PR #96](https://github.com/jasonyandell/texas-42/pull/96),
   `codex/nello-counterexamples` → `codex/nello-player`: opt-in defender
   refinement, completed-round checkpoints, offline research and wiki synthesis.
   It incorporates the updated #90 head. Merge #90 with ancestry preserved,
   then retarget #96 to `main` and confirm its remaining diff. No PR is merged
   or set to auto-merge by this preparation.

The current browser settings remain 350 explicit deeper worlds, three pools
of 540 counterexample candidates, up to four retained failures per round, and
a 4.2-second reservation inside the same 14/20-second total deadline. The shared
API defaults counterexamples off. Plunge owns the single default-off
**Nel-O · Preview** feature setting and enables all Nel-O defender analysis
together when selected.

## Validation of the combined candidate

All executable checks below ran under the existing 295-second process-group
watchdog and finished. Each receipt records the exact invocation and output.

| Check | Outcome |
|---|---|
| Mechanics PR #90 independently | Nine player/contract/Nel-O tests passed on its updated head |
| `walt-player --all-targets` release tests | 14 passed, including contract, Nel-O, counterexample and offline probe tests |
| Affected core regression suites | 66 passed, one existing ignored test: rules, Scheme, compact dice, policy, hand cache, sigma1 repair and semantic tables |
| Counterexample module | Three tests passed natively and the same three without default features |
| GPU reference regressions | Eight passed across `m1`, `prose_bridge`, `same_context`; straight declaration encoding retains its nine-declaration scope |
| Workspace/all-target compilation | Passed |
| Strict Clippy for shared player, live table/probe binaries and contract/Nel-O/counterexample suites | Passed with warnings and float arithmetic denied |
| Native/WASM Nel-O conformance | 96 positions, exact option vectors, legal checkpoints and input refusals passed |
| Counterexample conformance | Native/WASM stress-vector parity, 350-world completion, reserved-time behavior and interrupted-round retention passed |
| Ordinary native/WASM conformance | Ten L1 vectors, partner-prefix behavior and deadline checks passed |
| Candidate versus approved preview WASM | All twelve ordinary comparisons passed, including exact vectors, counters and full partner review |
| Auction native/WASM/job/merge conformance | Passed after the parity-predicate lint fix, including nine-declaration prices, higher targets, ties and deadlines |
| Independent Python Nel-O, replay, bridge and table adapters | 23 passed |
| Research archive | Sample ladder and counterexample summaries regenerated byte for byte; all six source-file hashes reproduced from the pinned historical source plus recorded replacements |

The large 100k study was **not rerun** for this documentation pass; its raw
cases, exact summaries, failed-command records and producers are now retained
in the [research archive](probes/nello-ruby-doubles-2026-09-21/README.md).
Generated large-budget WASM binaries remain local; their hashes, source identity
and exact cap/time replacements are retained. No such cap change enters the
shared player being merged.

## Check limits and remaining research

This is **not a green full legacy CI claim**. The workspace formatting check
is not clean; the retained log includes pre-existing formatting debt such as
`walt/src/gym.rs`, unchanged from `main`. No broad formatting rewrite was made.
All-target player Clippy also reaches two unchanged Kiln utilities:
`kiln-scheme-contrast-worker.rs` (`manual_is_multiple_of`, `map_entry`) and
`kiln-decision-worker.rs` (`manual_is_multiple_of`). They are byte-identical to
`main`. Scoped Clippy covering the live player and Nel-O additions passes.
The initial auction-lint failure and subsequent broader failures are retained
alongside the successful scoped check. The full workspace/Lean/receipt gate was
not run; the affected compilation, mechanics and parity checks are explicit above.

The prior defense screen and held-out counterexample panel do not establish
stronger play. A compute-matched fresh-root experiment with live subsequent
replanning remains the next research step. No new formal claim, exact natural
doom mass, or physical-phone latency benchmark is asserted.

## Plunge boundary

[Plunge PR #4](https://github.com/jasonyandell/plunge/pull/4) and its
[preview](https://plunge-pr-4.texas42.workers.dev/?nello=preview) remain separate.
The approved deployed build is `fbd8c02628ae9caa6f056b99d9367737fcada3d4`,
pinning Texas 42 source `cb1ef3b23072e4c268f31f625f2b61d5facc1929`.
That build's 238 app tests, deployment checks and 5.20-second Chrome replay
spot check are historical evidence from 2026-09-24, not new device measurements.

This task does not import a new Plunge WASM, alter its production flag, or deploy
the merged source. A later app release should commit the desired Texas 42 source,
run Plunge's importer, and validate the matched source/WASM manifest and preview
before publishing. The current source's equivalent auction lint fix passed
auction conformance and ordinary parity against the approved preview artifact.
