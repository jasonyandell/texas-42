[package]
name = "walt-m3-net"
version = "0.1.0"
edition = "2021"
license = "UNLICENSED"
publish = false

[dependencies]
walt-core = { path = "../walt-core" }
walt-gpu-spec = { path = "../walt-gpu-spec" }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
float_arithmetic = "deny"

