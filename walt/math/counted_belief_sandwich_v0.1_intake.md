# Intake companion — Counted Belief Sandwiches and the Refinement Calculus

**Parent (verbatim, never edited):**
`walt/math/counted_belief_sandwich_v0.1.md`
SHA-256 `4d2dfbe0fd9a5fab1d555cda9095ee93f6b82f0755545070008df1cb190eab7c`
(pinned by the `.sha256` beside it).
Companion verifier (shipped beside the parent, filed verbatim):
`walt/math/verify_counted_belief_sandwich_v0.1.py`
SHA-256 `ff87fb67058ed88ad3e7255e46759df46233f32828ed6a5a081f801d5545858f`.

**Status of this document:** exploratory intake record, maintained under
dated provenance markers. The verifier is **scratch tier at best**: useful,
a reference, never a candidate for import into the codebase; its PASS
lines are session evidence, not receipts (TRUST-01).

**Provenance:** a Pro deliverable, hand-delivered by Jason 2026-08-29/30
("new math buddy. I'm pretty excited about this"). **Not posed from the
courier machinery** — no dispatch number corresponds to it and the
exchange ledger is untouched (the calculated-evidence precedent). Same
side-channel lineage as the two 2026-08-24 intakes; adjudicated
2026-08-30 under the standing same-lineage go, per Jason's explicit word
this session ("it goes through the intake process"). The parent
self-describes its filename as `DESIGN-walt-counted-belief-sandwich-v0.1.md`
(its verifier's docstring names the same); it is filed under the
`walt/math/` snake_case convention — recorded here, not repaired.

**Thread labels (CE = sampling depth, L2 = model choice):** Parts I–II
are **[L2 consuming CE machinery]** through the sanctioned
one-directional crossing — root best response under a fixed field,
bounded by the CE one-mean evidence engine. Parts V–VI are **belief-
representation substrate** (the kernel/fiber authority lineage — neither
a sampling-depth nor a model-choice claim). Parts III–IV and VII–XII
serve both and are labeled per use.

**Snapshot check:** the parent declares its engineering inspection
against main `a1d2219ba22270f1c789ca1892c85f93e3cd7d42` — verified at
intake to be the current main (the walt2-wasm wiki sync). Every file its
"engineering sources inspected" list names exists at that snapshot.

## Adjudication receipt (what was actually re-run)

- **Verifier executed** (stdlib Python 3, exact `fractions`/integers
  only in correctness logic; run with `-I -B` from the filed location,
  no transients left): **20/20 PASS, exit 0** (`ALL CHECKS PASS`).
  Includes the exhaustive Part-1 sweep — all 256 two-policy Boolean
  tables on four worlds × all 256 length-four streams (65,536 stream
  evaluations), worst finite-horizon undercoverage **11/128 < δ = 1/4**
  for both the max-preserving upper and the fixed-policy lower — the
  same engine shape the x:024 intake swept; plus the strict E3-vs-fused
  specimen (Q = 1/2, E3 = 3/4, fused = 1), the grammar/residual and
  cylinder-partition identities, the 90-deal three-seat factor-belief
  closure (Z = 282 both ways; factorized Bellman = explicit policy
  optimization at value 30/47), the cell calculus, the threat/safety
  bound specimen, and the three-stage CEGAR fixture. Session record
  2026-08-30.
- **Caveat kept in view:** the sweep is a finite-horizon sanity check on
  toy domains; the *anytime* property of Theorem 5.1 rests on the
  step-checked proof (and on the already-adjudicated x:024 P1–P3
  ledger), not on the program.
- **Opening-scale arithmetic re-derived:** C(21,7) = 116,280,
  C(14,7) = 3,432, product **399,072,960** — §22 exact.
- **Theorems step-checked sound:** 2.1 (survivor soundness), 5.1 + 5.2
  (see the identity finding below), the §6 discovery/evaluation lower
  route, the §8 partition non-gain identity, 9.1 (cylinder partition),
  the §12 grammar/residual decomposition, §13–14 cell aggregation, the
  §16 pointwise threat/safety inequality (verified by four-case
  analysis), 17.1 (refinement monotonicity), 20.1 (posterior closure —
  Bayes plus seat-locality, sound under its five declared boundaries),
  §21 (branch-mass ratio), 23.1 (factorized Bellman, finite-horizon
  induction), 30.1 (CEGAR safety), §38 (finite completeness).
- **The identity finding — Theorem 5.1 is already adjudicated
  mathematics:** it is Theorem M1 + Corollary M2 of the x:024 response
  (TRIPLE-A2, 2026-08-25), restated over `Π_a` with the pmake
  objective: same one-fixed-maximizer argument, same
  endpoint-monotonicity collapse to a single integer, same
  no-Bonferroni conclusion, same nested-prefix minimum. The
  implementation exists in `walt/walt/src/solver/upper_cs.rs` with the
  ScopedDelta risk arithmetic. Part II therefore introduces **no new
  statistics** — it generalizes an adopted construction to a new
  objective.
- **Code-boundary audit:**
  - `sampled_split_reach` (`solver/exposure.rs`) computes the true
    information-consistent empirical optimum **for the split-reach
    objective** (verified at the x:024 intake; the
    same-action-per-public-node invariant). The parent's §44 step 1
    ("run the existing exact-on-prefix information-consistent solver
    to obtain S*") **overstates what exists**: the pmake-objective
    sampled optimizer — the declared-prefix sibling of
    `exact_root_value` (exposure.rs), with branches run to decided
    terminals — is **green field** and must be built (CBS-A2).
  - Seat-locality of the shipped field models (Theorem 20.1 boundary 2)
    **verified in code**: a modeled level-k mind's action is a pure
    function of the acting seat's own hand, the public record, and
    frozen identity/seeds (the `pi_cache` key and the
    `INNER_SEED ^ level_tag ^ mix(seat) ^ mix(hand) ^ record_hash`
    formula, `solver/mod.rs`). No shared hidden tape couples seats.
  - §25.1's "current kernel" tile-pattern dynamic program lives in
    **`walt/walt/src/kernel/fiber.rs`** (`FiberDp`: pool tiles grouped
    by allowed-slot bitmask, capacity-respecting layer counts,
    multinomial per group), not `kernel.rs` — attribution amended
    (CBS-A6). It is exactly the uniform-root special case of the
    proposed `ExactCoverOracle`, which is the intake's happiest
    finding: the existing counting authority already *is* the
    factorized belief at the uniform root.
- **Probe-citation audit (§0, the executive ruling):** the parent's
  four probe summaries were checked against the probe READMEs. Waking
  is fully supported (σ0 baseline 729‰ of decision compute, wake check
  269‰, escalation 1‰ — the honest contrast is three-way, and from
  trick 4 on the wake check dominates). The other three **soften the
  READMEs' own verdicts**: ordering's verdict is "wall-clock deltas are
  inside single-shot noise" (not a partially-consumed gain);
  field_cache's headline is "the levers do not rescue the convicted
  regime on CPU" (trick-1 ~4%; the 2.2× lives at the fiber-90 root);
  bundle's headline is "**the claim is NOT confirmed at this slice**"
  (mechanism real — ~5–6× fewer nodes, ~2.1× fewer field queries —
  but wall ~1.04× on the realistic field and *slower* than per-world
  on the trivial field). The corrected record **strengthens** the
  parent's thesis: not "gains were consumed" but "no confirmed wall
  gains at all in the convicted regime." Ruled at CBS-A8: the READMEs
  remain the only quotable authority for probe results.
- **Vocabulary sweep:** zero occurrences of the banned bare
  "certificate"; zero float-type mentions in the parent; support/belief
  and feasible/reachable used with their typed meanings (the parent's
  own vocabulary preamble is exemplary). One collision found:
  **"sandwich"** is already load-bearing in adjudicated names — the
  decision-sparse Theorem E6.3 value sandwich, the **refuted** T1-A
  trick-1 bounded sandwich, the SP-A7 sandwich discipline. Resolved by
  adopted working vocabulary at CBS-A3 (**root interval** / **survivor
  set**); "counted belief sandwich" survives only as the parent's
  title.

## Verdicts by part

| Part | Content | Verdict |
|---|---|---|
| I (§1–3) | Root intervals, survivor soundness, monotone refinement | **SOUND.** Theorem 2.1 elementary and correct; result typing extends the CE six-way ladder. |
| II (§4–8) | Optimization-lock upper, lower witnesses, partition non-gain | **SOUND; not new.** Theorem 5.1 = x:024 M1/M2 over pmake (see identity finding). §6's same-stream-argmax refusal and §8's non-gain identity both correct and load-bearing. |
| III (§9–12) | Policy cylinders, branch-and-bound, grammars, residuals | **SOUND.** The §11 non-theorem (pointwise fusion is not lawful coverage) is the O34 strategy-fusion fence restated; grammar coverage comes only from a residual bound. |
| IV (§13–17) | Counted cells, pairwise calculus, threat/safety, refinement | **SOUND.** §16 verified by four-case pointwise analysis; connects the existing zero-hazard machinery (`solver/hazard.rs`) to nonzero covers. |
| V (§18–23) | Factorized posterior, closure, branch masses, Bellman | **SOUND — the genuinely new mathematics.** Closure boundaries verified against shipped fields; 3,432× representational collapse at the opening root is exact arithmetic. |
| VI (§24–26) | `ExactCoverOracle` interface, candidate backends | **ADOPTED as interface design.** §25.3 subset convolution correctly flagged as candidate, not performance claim; §26's measurement separation adopted. |
| VII (§27–31) | Consequence CEGAR, critical tiles | **SOUND.** Witness-required refinement; heuristics propose, verifiers decide — the contagion framing made rigorous. |
| VIII–IX (§32–40) | Unified solver, steering, completeness, failure modes | **SOUND.** §38 completeness correct and honestly labeled in-principle; the seven failure modes adopted as the diagnostic vocabulary. |
| X (§41–43) | Engineering architecture | **ADOPTED with attribution amendment** (fiber.rs, CBS-A6); §43's cache-identity warning is the PiKey lesson, binding. |
| XI (§44–50) | Slice program A–G | **ADOPTED as the build program** (CBS-A9); §44 step 1's "existing" amended (CBS-A2). |
| XII (§51–53) | Experiments and falsifiers | **ADOPTED.** Falsifier column present for every experiment — the honest-failure discipline built in. |
| XIII (§54–55) | Proof obligations CBS-O1..O15, Lean module map | **ACCEPTED** into the Lean side-project ledger (Jason + Pro track). |
| XIV (§56–57) | First-session acceptance contract, stop conditions | **ADOPTED** (CBS-A9); §57 stop conditions verbatim; one already discharged at intake (the S* solver exists for split reach; the pmake variant is an honest build item, not a discovered absence). |
| XV (§58) | Synthesis | The governing principle ("which unresolved mass can still change the root decision") adopted as the steering statement. |

## Adjudication

Ruled same-day at **CBS-A1..A9** (`walt/CENSUS-RULINGS.md`, § "The
counted-belief-sandwich adjudication (2026-08-30)"). Indexed on
[walt-math-intakes](../../wiki/walt-math-intakes.md). The first build
session (Slice A + Slice C skeleton) is queued on Jason's word; the live
default player is untouched by everything in this lineage until arena
and conformance gates say otherwise (CE-A7/§20.16, restated at CBS-A9).
