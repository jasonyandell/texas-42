# Intake companion — Anytime Proof-State Walt (score calculus)

**Parent (verbatim, never edited):**
`walt/math/anytime_proof_state_score_v0.1.md`
SHA-256 `7a8c60fba5a37f6d8d4451cbf55d478f9d7faaf401b13e724e98c261df9d2e8b`
(pinned by the `.sha256` beside it).
Companion verifier (shipped beside the parent, filed verbatim):
`walt/math/verify_anytime_proof_state_score_v0.1.py`
SHA-256 `9efa51a017ac793b60f8291a6b695db9be54bb86b3ca64c828ca218bb2a5eca3`.

**Status of this document:** exploratory intake record, maintained under
dated provenance markers. The verifier is **scratch tier at best**: useful,
a reference, never a candidate for import into the codebase; its PASS
lines are session evidence, not receipts (TRUST-01).

**Provenance:** a Pro deliverable, hand-delivered by Jason 2026-08-31
("new math intake buddy. this is a biggie."). **Not posed from the
courier machinery** — no dispatch number corresponds to it and the
exchange ledger is untouched (the calculated-evidence precedent; same
side-channel lineage as the counted-belief parent). Adjudicated same-day
under the standing same-lineage go plus Jason's explicit in-session word
("standard protocol on an intake from pro"). The parent's verifier
docstring self-names the parent
`DESIGN-walt-anytime-proof-state-and-score-calculus-v0.1.md`; it is
filed under the `walt/math/` snake_case convention — recorded here, not
repaired.

**Thread labels (CE = sampling depth, L2 = model choice):** the parent
is an **orchestration-layer design** sitting above both threads. Its
sampled facts (§27, the δ-qualified proof states) consume the CE
one-mean machinery through the sanctioned one-directional crossing; the
fixed modeled field remains an L2 model choice named in every semantics
identity (§51). The score layer itself (Parts I–V) is
representation-substrate mathematics — neither a sampling-depth nor a
model-choice claim — labeled per use downstream.

**Snapshot check:** the parent declares its engineering basis as `main`
at `25b40d9f4243e4c7941d4324ed9859c68b943843` — verified at intake to be
the current main (the Slice G merge, PR #69). Its §0 characterization of
Slices C–G (eight capabilities, one negative, one refusal) was checked
against the landed slices and is accurate; see the code-boundary audit.

## Adjudication receipt (what was actually re-run)

- **Verifier executed** (stdlib Python 3, exact `fractions`/integers
  only in correctness logic; run with `-I -B` from the filed location,
  no transients left): **36/36 PASS, exit 0** (`ALL CHECKS PASS`).
  Includes the full 256-signature census (checks 1–3: every integer
  score 0..42 represented), the exhaustive rescue/fragile-band sweep
  (checks 11–12: all 13×5×13×13 grid points of the uniform inclusion),
  the 65,625-case certified-regret sweep (check 15), the
  threshold-envelope non-realizability specimen (checks 18–20), the
  merge-before-max counterexample (check 23), the laydown quantifier
  hierarchy (checks 26–27), and the closure-aware scheduling
  counterexample (check 31). Session record 2026-08-31.
- **Weak checks kept in view:** check 14 (loss-from-perfection) is
  near-tautological as coded — the theorem's real content is the
  one-line inequality, step-checked below; checks 16–17
  (proof-vs-executable bar) and 32 (selection-vs-value debt) are
  **definitional illustrations, not theorem verifications**; check 4
  does its work in a loop of bare asserts and reports once. None of
  this weakens the parent — the load-bearing claims are elementary and
  were step-checked directly.
- **Theorems step-checked sound:** the §3 tail-sum identity
  (E[S] = Σ_k Pr(S≥k); mass form); the §7 contract-sensitive width
  identity (U_c − L_c = straddling mass, from the indicator
  difference); the §8 integrated-width identity (P_ρ = Σ_k W_ρ(k), by
  integrality of scores); §9 monotone refinement; the §10/§11
  rescue-band and fragile-make theorems (worldwise inclusion
  arguments); §12 cellwise gain bounds (see the wording caveat below);
  the §14 positive-part bound (an integral crossing gains ≥ 1 point,
  so the indicator is dominated by D⁺); §15 loss-from-perfection
  (S ≥ P−d, d ≤ P−c ⇒ S ≥ c, inclusive because making exactly the bid
  succeeds); the §16 laydown hierarchy (four strictly separated
  quantifier types — strictness witnessed by checks 26–27); the §18
  fixed-policy profile recursion (branchwise addition, mass
  conservation Σ_s H = Z); §19 (the grammar recursion carries **one
  whole child profile** chosen by tail mass under a declared tie rule
  — one lawful policy, never a componentwise mixture); the §20
  non-realizability counterexample (re-derived: envelope tail-sum
  63/2, attained by neither expected-21 policy); the §22 interval
  recursion over unresolved classes (unresolved mass R joins every
  upper tail, joins lower tails only up to a proven ℓ_R); §23
  merge-before-max; §26 closure requirements (reductive + idempotent —
  requirements on an implementation, not theorems); §27 risk
  semantics (deterministic refinement of a δ-sound state consumes no
  new risk; exact replacement does **not** retroactively make earlier
  probabilistic exclusions exact — matches Slice G's `delta_decisive`
  semantics precisely); §28 decision exactness as projection
  exactness; the §29 survivor theorem; §30 B^exec ≤ B^proof ≤ Q*; §31
  certified regret and its monotonicity (U* never rises, B^exec never
  falls); §32 local vs global regret; §§34–38 debt definitions
  (consistent, distinct); §41 closure-aware usefulness (the
  counterexample is check 31); §42 steering bounds (safe,
  conservative); §43 (a complete exact solve is one work item — the
  iterative controller **contains** the exact solver).
- **Wording caveat (§12):** the first boxed cellwise formula's
  "on the relevant subcell" is informal; the operative statements are
  the exact-incumbent form and the envelope form
  (U_R(c) = Σ μ(C)·1{u_C + d_C⁺ ≥ c}/Z) that follow it, both sound.
  The companion governs: cite those two, not the informal box.
- **Identity findings (already-adjudicated mathematics, correctly
  inherited):** the §29 survivor theorem is CBS Theorem 2.1 — Slice
  G's shipping law; §23 merge-before-max is the CBS §20/23 posterior
  closure discipline and the O34 strategy-fusion fence, restated over
  score profiles (its counterexample, check 23, is the O34 specimen in
  score form); the §5 banked/unbanked envelope b ≤ S ≤ b+r is Slice
  D's monotone decided cutoff, generalized to an initial proof state;
  §26 zero-cost closure generalizes the G controller's
  bar/exclusion/settlement recomputation loop. The **genuinely new
  mathematics** is the score layer between behavior and contract:
  score profiles and tail envelopes, the contract-sensitive residual
  mass W_ρ(c) as the steering object, rescue and fragile-make bands,
  count-threat covers, the typed laydown hierarchy, the proof-bar /
  executable-bar split with certified regret, the debt taxonomy and
  declared solve goals, closure-aware usefulness, and the persistent
  resumable proof state as the primary theorem object.
- **Code-boundary audit** (all at main `25b40d9`):
  - §46's characterization of `solver::refine` verified against
    `walt/walt/src/solver/refine.rs` item by item — one root per call,
    scalar pmake intervals, closed work-item enum, ephemeral state,
    width steering, no policy-region graph, no score algebra, no
    dependency closure, no serialization, no goal taxonomy. Accurate,
    and correctly framed as declared scope, not defect.
  - **The bar finding:** `bar_of` (refine.rs:458) maxes over **all**
    installed lowers including `ExactGrammar` — the current
    controller's bar is exactly the parent's **proof bar**, and a
    grammar lower raises it with no extracted executable policy. This
    is sound for what G outputs (action selection; exclusion is
    licensed by the proof bar per §29–30) — G never recommends a
    policy off a grammar lower, so nothing shipped violates the
    §30 distinction. The executable bar becomes load-bearing exactly
    when the output grows a recommended-policy field (Phase 3).
  - **Selection-debt variation:** G's `decision_width`
    (refine.rs:475) sums excess (U_a − B) over **all** survivors
    including the bar holder; the parent's §34 excludes the canonical
    bar holder. Both are monotone under refinement and agree at
    settlement; recorded as a definitional variation, not a conflict.
    Neither number is comparable across the two definitions.
  - §0's list of eight C→G capabilities, the Slice F fragmentation
    negative, and the §39/§41 census-refusal story all verified
    against the landed slices (gate 3 of
    `tests/solver_factor_refine.rs` asserts the census is refused and
    never runs). Check 31 is the honest counterexample showing G's
    "presently useless" rule is complete only in G's **closed**
    producer universe — with an open registry, usefulness must be
    evaluated after closure (Ĉ₀ ∘ F_W), or via declared macro-plans.
- **Vocabulary sweep:** zero occurrences of the banned bare
  "certificate" ("certified regret" / "certified ε-optimal" is a
  distinct term of art and does not collide with the D3 ban — checked
  deliberately); zero float-type mentions; "sandwich" appears only in
  the parent-title citation of the counted-belief parent (the CBS-A3
  working vocabulary — root interval / survivor set — is used
  throughout, as adopted); the parent's own vocabulary fence
  (undecided ≠ incorrect, heuristic ≠ bound, envelope ≠ executable
  policy, action ≠ score ≠ contract uncertainty, certain make ≠
  laydown) is exemplary and adopted verbatim. **"Laydown" enters the
  working vocabulary as a typed hierarchy** (APS-A5): bare "laydown"
  is reserved for the universal quantifier; the other three types are
  always named in full.

## Verdicts by part

| Part | Content | Verdict |
|---|---|---|
| 0 | Executive ruling: the C→G boundary, behavior→score→contract | **ACCURATE** on the engineering record (rare — no probe softening found); the boxed chain adopted as the steering statement. |
| I (§1–5) | Score game, exact profile, tail-sum identity, 42 signature, initial envelope | **SOUND.** 256-signature census verified; the 96-bin (τ,n₅,n₁₀) erasure caveat (legality/signaling) is correct and binding. |
| II (§6–9) | Cell projection, contract-sensitive residual W_ρ(c), integrated width, monotone refinement | **SOUND — the steering heart.** The three unresolved states (action-ambiguous / contract-stable / contract-sensitive) adopted as vocabulary. |
| III (§10–14) | Rescue bands, fragile makes, cellwise caps, count-threat covers, positive-part | **SOUND.** `CountThreatCover` adopted under the CBS-A7 discipline: producers may decline; only verified covers carry authority. |
| IV (§15–17) | Loss from perfection, typed laydown hierarchy, laydown closure | **SOUND & ADOPTED** (APS-A5). No sampled route may construct any laydown type. |
| V (§18–23) | Score-profile Bellman: fixed, grammar, interval, merge-before-max | **SOUND.** §20's envelope-is-not-a-policy fence BINDING (APS-A4); §19 constructs one lawful policy per contract. |
| VI (§24–28) | Proof state, top state, zero-cost closure, δ-states, decision exactness | **SOUND** as calculus; closure properties are implementation obligations (Phase 1 gates). §27 matches shipped `delta_decisive` semantics. |
| VII (§29–33) | Root intervals, proof vs executable bar, certified regret, fallback | **SOUND.** §29 = CBS 2.1 (identity finding); the bar split and Γ^exec are the genuinely new deliverables (APS-A6, APS-A7). |
| VIII (§34–39) | Debt taxonomy, declared solve goals | **ADOPTED** (with the selection-debt variation recorded). Goals prevent unlike objectives collapsing into one scalar. |
| IX (§40–44) | Work transformers, closure-aware usefulness, steering bounds, resumability | **SOUND.** §41 AMENDS the steering doctrine (APS-A8); §43 keeps the exact solver inside the schedule. |
| X (§45–49) | Architecture: keep Walt as authority, fresh orchestration core, spike gate | **ADOPTED AS CANDIDATE** behind the §49 spike (APS-A9). Never a second Walt; adapters only. |
| XI (§50–56) | Proof-state data model | **ADOPTED as design skeleton** — types are proposals until the spike; provenance DAG (§56) binding whenever built. |
| XII (§57–65) | Phases 0–8 | **ADOPTED as the build program**, queued on Jason's word (APS-A9). Phase 0 is doc-and-adapter work only. |
| XIII (§66–68) | Acceptance conditions | **ADOPTED verbatim** — the 14 mathematical + 12 engineering conditions are the round's gate language; the §68 probe roster is the fixture shopping list. |
| XIV (§69–74) | Falsifiers and honest negatives | **ADOPTED.** Every falsifier names a result, not a retreat from proof types. |
| XV (§75–76) | Lean program PS-T1..T15 + 42 instance layer | **ACCEPTED** into the Lean side-project ledger (Jason + Pro track). |
| XVI | Synthesis | The two boxed principles (refine the mass that can move the score across the contract; undecided is the default theorem object) adopted as steering statements. |

## Adjudication

Ruled same-day at **APS-A1..A9** (`walt/CENSUS-RULINGS.md`, § "The
anytime-proof-state adjudication (2026-08-31)"). Indexed on
[walt-math-intakes](../../wiki/walt-math-intakes.md). The build program
(Phase 0 spike first) is queued on Jason's word; `solver::refine` is
frozen semantically as the **RefineV1 reference** the new core must
reproduce; the live default player is untouched by everything in this
lineage until arena and conformance gates say otherwise (CE-A7/CBS-A9,
restated at APS-A9).
