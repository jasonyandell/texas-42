# Intake companion — Model-Belief Walt and the Closing of the First Book

**Parent (verbatim, never edited):**
`walt/math/model_belief_base_player_v0.1.md`
SHA-256 `1ffabf86e3d46d81c78627e712ecf8663f5cdf9075d19fca6f33038e7836c1bd`
(pinned by the `.sha256` beside it).
Companion verifier (shipped beside the parent, filed verbatim):
`walt/math/verify_model_belief_base_player_v0.1.py`
SHA-256 `b5c81fb62f0171ddd1f396cd8f867b26e8388e80b510cafdefcde761b9303356`.

**Status of this document:** exploratory intake record, maintained under
dated provenance markers. The verifier is **scratch tier**: session
evidence, never a receipt, never imported into the codebase (TRUST-01).
This companion **governs wherever it repairs or narrows the parent**
(the DS-A17 citation rule).

**Provenance:** a Pro deliverable, hand-delivered by Jason 2026-09-01
("big math intake coming... this next round is about intake and
collaboration"). Not a courier dispatch — no number, ledger untouched
(the calculated-evidence precedent). The upload arrived as
`THEORYwaltmodelbeliefbaseplayerv0.1.md`; filed under the `walt/math/`
snake_case convention — recorded, not repaired. Adjudicated same-day
with Jason in the loop ("overall, agreed on your rulings"); build
authorized same-day ("full go buddy").

**Thread labels (CE = sampling depth, L2 = model choice):** primarily an
**L2-thread design** — the field model itself becomes a hidden latent
coordinate, and every point-mass field remains a named L2 model choice
inside the belief. Sampled point-mass components consume CE machinery
only through the sanctioned crossing: §20's rule that the risk ledger
must cover every sampled claim a weighted result consumes.

**Snapshot check:** the parent declares its basis as `main` at
`08fe3d2ddca2a17cf8faf633add69d3f83592ec5` — verified at intake to be
the then-current main (the wiki-first-pass merge, PR #80). Its §1
inventory of the current system (twelve capabilities from canonical
fiber to honest unresolved output) checked against the landed record;
accurate. Its restatement of the opening-root figures (~732‰ floor,
~999‰ upper, ~267‰ unclaimed, honest UNRESOLVED) matches the Phase 8
probe record.

## Adjudication receipt (what was actually re-run)

- **Verifier executed twice** (stdlib Python 3, every probability an
  exact `fractions.Fraction`): once from the upload, once from the
  filed location — **40/40 PASS, `ALL CHECKS PASS`, exit 0** both
  times. Session record 2026-09-01.
- **Load-bearing checks:** the §1 sweep (all 64 Boolean 2-policy ×
  3-type value matrices × 3 priors: point-mass upper dominates the
  mixture optimum; the type-partition lattice has correct endpoints
  and refinement monotonicity over all 5×5 partition pairs of a
  3-type space; convexity of Q in ν; the zero-fusion-price iff
  common-pointwise-optimizer equivalence checked as an exact
  biconditional per matrix); the §4 sweep (all 256 two-policy ×
  two-world × two-type Boolean utilities: field-disagreement outcome
  mass bounds both fixed and optimized model transfer — the §34
  bound); §2's nested-vs-flat augmented-atom identity; §3's Bayes
  closure on a 3-hand × 2-type factor (update multiplies only the
  acting factor; normalizer = observed-action mass); the
  persistent-vs-resampled ½ vs ¼ separation; §5's law-of-total-
  expectation conservation under public-action splitting.
- **Weak checks kept in view:** several checks are **definitional
  illustrations, not theorem verifications** — "Dice and ladder rungs
  fit one finite model registry" (asserts 4 == 4), "contract-stable
  unresolved behavior need not widen pmake" (asserts 1−1 == 0),
  "persistent model belief creates positive value of information"
  (asserts 1 > ½ on hardcoded values), "model agreement produces no
  extra public branch" (a set-cardinality pun). None carries weight;
  the corresponding claims are either elementary (VOI ≥ 0 is policy-
  class inclusion, MB-O18) or engineering obligations. Recorded so
  the check count is never quoted as 40 theorem verifications.
- **Theorems step-checked sound:** Thm 7.1 (augmented-world
  reduction — conditional on ξ every nonfocal action is determined,
  so the problem is fixed-semantics Walt on Ξ; the lifting
  consequence holds for every proof using only finiteness, lawful
  focal information, bounded utility, fixed latent distribution);
  Thm 12.1 (posterior closure — seat-local likelihood absorbs into
  the acting seat's factor); §13 exact branch masses Z_ht/Z_h; §15
  residual envelope; Thm 17.1 (convexity); Thm 18.1 (sep upper);
  Thm 19.1 (zero fusion price iff one lawful policy is pointwise
  optimal on the support — positive weights force every summand to
  zero); §20 weighted interval combination; Thm 25.1 (type-partition
  lattice); §32 merge-before-max; §34 transfer bounds (via
  |max f − max g| ≤ max|f−g|); §35 VOI nonnegativity.
- **Transcription errata (MB-A4; the parent is checksum-pinned, so
  recorded here, never repaired there):**
  - §8's display `Q_a(δ_{F_k})` is truncated mid-equation. Intended
    and adopted statement: **Q_a(δ_{F_k}) equals the ordinary exact
    best response to rung F_k** — point priors reproduce fixed-field
    Walt.
  - §34's indicator prints `u_ρ(ω,θ) e u_ρ(ω,θ₀)`. The operator is
    **≠**: r_ρ(θ,θ₀) = Pr_ω(u_ρ(ω,θ) ≠ u_ρ(ω,θ₀)), the outcome-
    disagreement mass. The verifier's §4 sweep tests exactly this
    form. Cite this companion's forms for both.
- **The rung registration (MB-A3, corrected at intake):** the parent
  writes D, F₀, F₁, F₂. Binding correspondence — D =
  `FieldModel::Dice` (uniform dice, frozen per-scenario draws;
  `level1.rs`, `playout.rs`); **F₀ = BR(D) = σ0**, the banked-correct
  level-0 modeled mind (`solver/field.rs`); F₁ = BR(F₀) = level-1
  walt; F₂ = BR(F₁) = the unbuilt level-2 rung. The registration
  first proposed at intake ("Dice = σ0") was wrong and is retracted;
  the §3 landmark claim about F₂ is consistent with this table.
- **Missing-parent resolution (MB-A2):** the fifth named parent, *The
  Salvation Complex and Information-Cut Calculus of Walt v0.1*, was
  absent at first intake and delivered the same session; intaken as
  `walt/math/salvation_complex_v0.1.md` with rulings SC-A1..A8.
  §§26–27, 39–40 and MB-O11 are grounded, not forward references.
- **Vocabulary sweep:** no bare "certificate" collisions ("certified
  regret" remains the APS term of art); no float-type mentions; the
  parent's typed distinctions — Bayesian model belief vs credal
  region vs local behavioral envelope (§29), persistent type vs
  action-wise resampling (§9), global behavioral equivalence vs
  current-action equivalence (§14) — are adopted vocabulary.

## Verdicts by part

| Part | Content | Verdict |
|---|---|---|
| 0 | Executive ruling: field model as hidden state; book-one close criteria | **ADOPTED** (MB-A1); the ten close criteria are the round's finish line, §49's non-requirements equally binding. |
| I (§1–4) | Where Walt stands; what doom did and didn't diagnose | **ACCURATE** against the landed record; the two-direction race (columns vs cuts) adopted. |
| II (§5–10) | Types, Ξ = Ω×Θ(×Z), reduction theorem, point-mass case, persistence, correlation | **SOUND — the substrate.** Thm 7.1 step-checked; persistence binding (MB-I2); the κ convention latent is a stated future interface, never asserted independent. |
| III (§11–15) | Hand-type factors, posterior closure, branch masses, behavioral quotients, residual | **SOUND.** Thm 12.1 is the CBS closure over (H,θ); quotient merging only under an explicit domain receipt; residual Other MANDATORY (MB-A5). |
| IV (§16–23) | Response vectors, convexity, sep upper, fusion price, column-and-cut, reuse | **SOUND — the working geometry.** Response-vector caching (reprice under any ν by dot product) is the round's cheapest large win. |
| V (§24–28) | Type partitions, gluing theorem, CEGAR, augmented salvation, coalitions | **SOUND**, grounded by the salvation parent; §26's not-a-cut fence binding (SC-A5 twin). |
| VI (§29–36) | Unresolved fields typed three ways, cylinders, sparse contingencies, frontier, transfer, VOI | **SOUND & ADOPTED.** The §33 model-disagreement frontier targeting rule ("no model-belief compute before a reachable field disagreement") is the scheduler's first law. |
| VII (§37–42) | Upper portfolio, augmented doom, masks, provenance, count filtration, fusion horizons | **SOUND.** Typed upper provenance (MB-I8) matches shipped practice; §41 count-aware filtration restates APS-A2's steering. |
| VIII (§43–49) | The gloriously boring base player, decision criteria, algorithm, library, close criteria | **ADOPTED as the deliverable definition.** §45's secondary ordering is a declared lexicographic rule, never silent. |
| IX (§50–61) | Engineering: no third Walt, identities, data types, slices M0–M7, BaseWaltV1 freeze | **ADOPTED with restructure** (MB-A8): MB0–MB5 supersede M0–M7 as the build sequence; §51/§52 types are proposals until their slice; BaseWaltV1 = frozen contract, not end of research. |
| X (§62–65) | Common-information partnership bridge | **ACCEPTED as future architecture** — book-two material; interfaces must not foreclose it; MB-O20 queued behind everything. |
| XI (§66–69) | The new book | Direction, no rulings needed. |
| XII (§70–73) | Obligations MB-O1..O20, MB-I1..I10, empirical questions, falsifiers | **ADOPTED verbatim** as the round's gate language; the §73 falsifiers determine base-player vs lab-instrument, never correctness. |
| XIII (§74–77) | First implementation session, report, go/no-go | **ADOPTED as MB0** with §76 verbatim as the gate between the vertical slice and the base-player program. |

## Adjudication

Ruled same-day at **MB-A1..A8** (`walt/CENSUS-RULINGS.md`, § "The
model-belief base-player adjudication (2026-09-01)"). Indexed on
[walt-math-intakes](../../wiki/walt-math-intakes.md). Build authorized
same-day: MB0 + U0 first (the two-sided race), serial fable agents
under per-slice briefs (`walt/briefs/`), `walt/FACTOR-BELIEF.md` the
running record. The live default player is untouched by this entire
lineage until arena and conformance gates justify a change on Jason's
word (CE-A7/CBS-A9/APS-A9 restated at MB-A7).
