# factory-results — relocated 2026-08-24

Formerly `walt/walt-factory/results/` (the tracked summaries and
certificates only) and `walt/walt-factory/docs/certificate-schema.md`,
relocated here when the walt-factory crate was archived by the
unification ([[walt-unification]]). Historical docs and wiki pages
cite the old paths; this directory is their referent. The untracked
bulk (8.2 GiB frontier body, 499 MB memo store, and friends) lives in
`~/data/texas-42/walt-factory-archive-2026-08-24/` and the private HF
dataset `jasonyandell/texas-42-walt-archive` — see `walt/ARCHIVE.md`
for the ledger and the recompute protocol (producer code pinned at
commit 648f93a). Exploratory tier throughout; estimates, never
receipts.
