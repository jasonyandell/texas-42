# Overnight report — 2026-07-27

One night, from two ingest snapshots to: a reconciled wiki, a receipt-verified
Rust engine with a playing AI and visual inspector, first Lean kernel theorems,
and five externally-adjudicated mathematical results — including real movement
on the corpus's flagship open problem.

## Mathematics (via ChatGPT 5.6 Pro exchange — all five CONFIRMED)

Each verdict: the response's own program executed unmodified (ALL_PASS) plus
three independent adversarial referees, all SOUND, zero broken steps.
Evidentiary tier everywhere: *exchange-adjudicated CONFIRMED* — cited as such,
never as corpus theorem or kernel proof.

| # | Result |
|---|--------|
| 001 | **OPEN-11 interval [26,46] → [35,46] bits**: certified disjoint family of 17,668,066,045 reachable supports (no-void 559,316,142 + called-void 8,387,350,664 + natural-void 8,721,399,239). Machine-hardened fallbacks: [34,46]; ≥30 bits. Validated split-zeta counting machinery is reusable. |
| 002 | **Outer language not tight**: witness (NT, (6,6,6), V₁={6}) passes all four outer checks yet is unreachable (450 generators, 425,520 traces, 0 realizers). New **fifth necessary condition** (follower-supply obstruction) — a route to tightening the 46-bit ceiling. |
| 003 | **OPEN-01 resolved — COLLAPSE**: the reduced viewer kernel is strictly finer than the future-equivalence quotient (r=7 vs r=6 witness; dead-cut lemma). Raw fold ordinal is not an injective memo coordinate. |
| 004 | **Transport theorem proved**: f_{t,u}(R_t)=R_u; reachable census collapses 9→3 declaration classes. Cocycle lemma closed same-day by finite check over 343 triples (`004-cocycle.py`). |
| 005 | **All 19 census integers reproduce** under independent audit; shared-provenance risk discharged. New: Burnside decomposition 136,514 / 2,156 / 35 → 23,842. |

Budget: 5/10 submissions spent; referees recommended zero follow-ups.

## Engineering (rob — Rust, all independently re-verified by main session)

- **Slice 01 complete**: S1 algebra → S2 objective machine → S3 cells +
  losslessness parity → S4 normal form + capacity DP. Every ingest receipt
  reproduced exactly (737,100 ×2; 81-bit census to the last digit;
  44,352,165 floor via legal witness prefixes replayed in rob's own machine;
  full list in `wiki/verification.md`). INV-1..10 enforced by type system,
  compile-fail doctests, CI greps, byte-diffed receipts.
- **Evening player v0** (Jason's spec): fixed-field MC best-response, §11.4.
  The one law is structural — rollout policy fixed before worlds are drawn,
  world identity unrepresentable in its view; exact uniform fiber sampling
  (rejection, no modulo bias); exact integer/rational values; seed-42 receipt
  match frozen (13 hands, T0 7–6 T1, 0.4 s/match).
- **Inspector**: `cd rob && open inspector/index.html` — 364 decision states,
  arrows to step, 0–3 per-seat perspective, O omniscient. Per-seat exact fiber
  counts, voids, marginals; actor decision panel with exact values and argmax.
  Masking emitted per-seat from Rust; JS recomputes no game logic.
- **Slice 02 assigned and running** (`rob/BRIEF_SLICE_02.md`): S5 matching-minor
  dynamics → S6 symbolic gate → S7 outer profiles → S8 unreachability
  regressions (REACH-10 + the 002 witness) → S9 transport/3-class census;
  S10 stretch = reproduce the 17,668,066,045 floor in Rust. New invariants
  INV-11..14 (63-edge budget, monotone ambiguity, typed-transition-only,
  five-checks-necessary-only).

## Foundations infrastructure

- **Wiki** (19 pages): reconciliation verdict "rec's mathematics under v0.7's
  type discipline"; 15 discrepancies resolved; claim ledger; all updated with
  tonight's five results at their evidentiary tier.
- **Lean 4 + mathlib** installed; first kernel theorems: `card Domino = 28`,
  `∑ countPoints = 35` (clean axioms; PA-A01/A02/A04).
- **Exchange channel**: hardened Playwright/CDP harness; `/pro-exchange`
  project skill; adjudication workflow (extract → execute → referee panel →
  verdict) now tuned to Jason's model policy (Opus referees, one xhigh verdict
  per question).

## Open items

- rob slice 02 in flight (reports per gate).
- cliclick Accessibility grant (System Settings → Privacy & Security →
  Accessibility) — only if the UI-automation fallback is ever needed.
- Next natural exchange targets (reserve 5/10): quantify the fifth condition's
  ceiling improvement; exact |R| via the 3-class + validated machinery;
  Lean-side theorem proving once statements are `sorry`-scaffolded.
- Buddy's successor and default agents move to Opus 5 per model policy.
